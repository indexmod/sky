```markdown
# Sky

A minimal interactive sky.

Day is blue with a white L-shaped frame.  
Click anywhere to switch to sunset.

No dependencies.

## Use

Open `index.html` in a browser.

## Controls

- Click — switch between day and sunset
- Enter / Space — switch between day and sunset
```